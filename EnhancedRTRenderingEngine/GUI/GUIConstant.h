#pragma once

enum class LayoutAttribute {
    Static,
    Dynamic
};

enum class UIWidgetType {
    ButtonLabel,
    RadioButton,
    IntProperty,
    FloatProperty,
    Label,
    ComboColor,
    ColorPicker
};

enum class UIAlign {
    LEFT,
    CENTER,
    RIGHT
};
